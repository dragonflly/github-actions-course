name: Format Example

on:
  workflow_dispatch:

jobs:
  format-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Use format() to create a custom message
        run: echo "${{ format('Hello, [{0}]! Today is {1}.', 'Alice', 'Tuesday') }}"
